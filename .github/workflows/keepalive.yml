name: Keep Supabase Alive

on:
  schedule:
    - cron: "0 12 */3 * *"  # every 3 days at noon UTC

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping keepalive endpoint
        run: curl -f https://pitch-compare.netlify.app/api/keepalive
